{
  "buildCommand": "turbo run generate --filter=@repo/database && turbo run build --filter=@repo/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}

